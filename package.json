{
  "name": "MapBox-Utils",
  "version": "0.0.1",
  "description": "Unofficial utility CLI for MapBox.",
  "author": "Gefferdemme B.V.",
  "license": "MIT",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "keywords": [],
  "scripts": {
    "start": "tsc && node ./build/src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.0.3",
    "@types/node": "^14.11.2"
  },
  "dependencies": {
    "aws-sdk": "^2.907.0",
    "axios": "^0.21.1",
    "commander": "^7.2.0",
    "ora": "^5.4.0",
    "pretty-bytes": "^5.6.0"
  }
}
